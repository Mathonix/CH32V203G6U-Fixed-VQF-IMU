# 工程审查与改进建议（revise）

> 审查日期：2026-09-23。范围：仓库中的应用代码、定点融合、定时/中断、通信、链接脚本、工程配置与 README；**仅静态审查**，没有连接实物、重新烧录或做波形/精度测试。下述风险与预期结果应按验证项复现，不应视为已经测得的故障。

## 总览

工程实现了 LSM6DSV → SPI → 定点 6D VQF → CAN/UART 的完整链路，并有传感器自检、通信计数器和定时统计。当前建议首先修复**可选量程的数据溢出**、**SPI 超时被当作成功**及**定时标志竞争**；其次使 1 kHz 配置的传感器实际 ODR 与算法时间基准一致，再考虑结构拆分、测试和性能优化。第三方 `Peripheral/`、`Core/` 及上游浮点 VQF 宜尽量保持原样，优先修改自有代码。

## P0：正确性与故障可观测性

### 1. ±4 g 输入与 ±2000 dps CAN 编码可能溢出

- **依据**：`User/main.c:490-499` 在 4 g 模式下使用 `(int32_t)raw * 131072L`。在 `raw` 超出约 ±16384 LSB 后，有符号 32 位乘法超范围；即使先用 64 位，Q1.30 的 `int32_t` 也无法表示超过约 ±2 g 的数。`User/main.c:576-585` 的 2000 dps 模式用 `raw * 7` 后直接转换为 `int16_t`，而 CAN 的 0.01 dps/LSB 只能覆盖约 ±327.67 dps，超出后输出会折返/截断（具体转换行为依编译器实现）。默认 2 g、125 dps 模式并不受同一范围问题影响。
- **建议**：输入换算先升为 `int64_t`，明确采用饱和到 Q30 可表示范围，或调整加速度定点格式/预缩放并同步审查所有滤波和归一化运算；CAN 如需保留 0.01 dps 格式则显式饱和并暴露饱和计数，若要表达完整 ±2000 dps 则必须重新定义协议比例并更新接收端及版本标识。避免未经检查的窄化转换。
- **验收**：两种量程分别做端点、正负满量程和边界值单元测试；CAN 解码不出现符号反转，异常输入触发明确的饱和计数。

### 2. SPI 轮询超时后仍发送/读取，可能把无效数据送入融合

- **依据**：`User/main.c:741-756` 的 `SPI1_Transfer` 在 TXE/RXNE 超时后仍分别写 `SPI1->DAT` / 读数据寄存器；`User/main.c:759-810` 的寄存器与突发读取接口均无错误返回，`LSM6DSV_UpdateData` 只依据状态寄存器位判定是否有效。
- **建议**：传输接口返回状态和接收字节；任一阶段失败时释放 CS、清理可能残留的 SPI 状态，向上传播错误；在主循环中计数、丢弃本次样本，并规定连续错误后的传感器重初始化/故障 LED 策略。超时采用确定的时钟/定时器上限，而非未标定的自减次数。
- **验收**：断开/阻塞 MISO 或模拟 SPI 超时，不产生“成功更新”计数，CS 最终释放，状态可通过诊断量观察，恢复后能重新取得有效样本。

### 3. 定时任务的单 bit 标志存在丢事件窗口

- **依据**：`User/ch32v20x_it.c:53-63` 在 TIM2 ISR 中设置 `vqf_sample_pending`；`User/main.c:895-900` 采用 `if (flag == 0) continue; flag = 0;`。若 ISR 恰好发生在检查和清零之间，本轮新到事件被覆盖，`vqf_missed_count` 也未必记录到它。`volatile` 只约束访问，不保证两次操作构成原子事务。
- **建议**：用关中断保护的“取走并清零”、或 ISR 单调递增 tick + 主循环消费序号；显式区分 ISR 溢出/积压、传感器未就绪和 SPI 错误的计数。若需严格等间隔积分，还应定义积压时丢样/补时的策略，而非将任意间隔都当固定 `Ts`。
- **验收**：在 ISR 与取标志的各种交错时序下验证每个 tick 要么消费、要么被明确记为丢失；长时间运行时核对 `vqf_timer_ticks`、`vqf_update_count` 与各类漏采计数。

## P1：时间基准、状态与通信

### 4. “1 kHz 模式”传感器配置实际标注为 960 Hz

- **依据**：`User/main.c:25-32` 的 `FIXED_VQF_SAMPLE_HZ == 1000` 分支注明 HAODR 960 Hz；但 `User/main.c:65-66,144-155` 定时 1000 Hz，`User/fixed_vqf.c:32-49` 的滤波系数、积分步长和静止阈值也按 1000 Hz 生成。`User/main.c:910-915` 在 DRDY 不满足时跳过，下一次有效数据仍按 1 ms 积分。
- **影响**：如果实际持续得到约 960 个新样本/秒，按 1 ms 更新会使累计积分时间与实际经过时间不一致；DRDY 轮询与采样相位也会影响输出新鲜度。具体误差需在板上核对 ODR、DRDY 和 tick。
- **建议**：采样节拍与器件实际 ODR 对齐，或给每次有效数据使用测得/明确约定的 `dt`，据此重新生成 LPF/VQF 参数；在 README 中明确 960 Hz 与 1 kHz 输出之间的关系。切换配置前先做时间戳实验。
- **验收**：分别在 2 kHz、备用 ODR 配置下测 DRDY、新鲜样本数/秒与定时 tick，并做已知角速度的长时间角度积分对比。

### 5. CAN “发送成功”计数仅表示进入邮箱

- **依据**：`User/main.c:543-574` 在 `CAN_Transmit` 返回有效邮箱后立刻增加 `can_tx_ok_count`，没有等待硬件发送成功/失败结果；`User/main.c:598-636` 即使提交失败也增加各类帧计数。
- **建议**：将计数明确命名为 `submit_count`/`no_mailbox_count`，如需真实成功率则在发送完成中断或邮箱状态机中统计 ack 成功、错误和重试/丢帧；报告总线离线、无应答、恢复事件。保留现有发送策略前先确认接收端对无应答时的容忍度。
- **验收**：接入正常节点、拔掉节点、使邮箱繁忙分别测试；“成功”指标不得在未确认总线发送时增加。

### 6. 失败路径缺乏统一状态与运行时恢复

- **依据**：`User/main.c:882-890` 的初始化中 CAN 失败仅设置 `can_error_status`，主循环仍运行；`User/main.c:813-869,954-964` 传感器自检失败后仅闪灯和继续调用 CAN service，不重新检测传感器；`User/ch32v20x_it.c:43-49` 的 HardFault 直接复位，没有保存最小故障上下文。
- **建议**：定义 `INIT / RUN / SENSOR_FAULT / COMMS_FAULT / RECOVER` 状态机和有限次数重试；按故障类型隔离输出，避免旧姿态误作实时数据；为 CAN/UART 输出增加可识别的有效性/在线状态（协议允许时）。故障记录可选用保留 RAM 并注意重启初始化流程。
- **验收**：传感器上电晚到、瞬时断线和 CAN 无应答时，故障可观测、旧值不会被误标为新值，设备恢复路径可复现。

## P2：维护性、测试与优化（在 P0/P1 验证后）

- **拆分模块**：`User/main.c` 约 969 行同时实现 SPI、传感器、自检、软件 LPF、Euler/CORDIC、CAN、UART、LED 与调度；建议拆为 `lsm6dsv.*`、`imu_pipeline.*`、`comms_can.*`、`comms_uart.*`、`app_diag.*`，对外只暴露配置、初始化与单次任务接口。保留计数器名的 GDB 兼容映射，避免一次重构改变外部协议。
- **纯函数测试**：优先为 `VQF_DegQ16_ToCentiDeg`、`VQF_Atan2_DegQ16`、两种原始数据比例、LPF 和四元数转换建立主机端测试；用上游 `User/vqf.c` 作为对照测试参考，而非贸然将其编进固件（`.cproject:265` 当前排除了它）。覆盖静止、动态、极值、±180° wrap、连续丢采；允许误差写成可执行阈值。
- **CPU/时序诊断**：`User/main.c:932-952` 用周期为 `VQF_PERIOD_US` 的 TIM2 计时；跨越超过一次周期时只补回一次周期，并且 `LSM6DSV_UpdateData()==0` 直接 `continue`，本轮不入统计。建议用独立单调计时器/周期计数器并统一记录成功、跳过与超时分布；先测最坏执行时间和空闲裕度，再决定是否需要优化 CORDIC、SPI 逐字节调用或缩减诊断开销。不要只依据平均 CPU 占用判断是否赶得上 0.5 ms 截止时间。
- **内存/链接布局**：`Ld/Link.ld:67-80` 注释称 `.slowtext` 在“非零等待 Flash bank”，实际仍链接到 `FLASH_FAST`；仓库中的 `obj/CH32V203G6U.map:1454` 也显示 `.slowtext` 从 `0x438c` 开始、在 32 KiB 区域内。应修正注释或在取得明确硬件分区证据后重新设计链接布局，切忌仅改段名当作性能优化。当前 map 是已有构建产物，不代表未来构建的资源占用。
- **构建可复现**：`obj/makefile` 为生成文件，当前链接命令带有旧机器的绝对 `c:\Users\Mathonix\mounriver-studio-projects\...\Ld\Link.ld` 路径；不能把它当成可移植构建入口。建议用项目相对路径提供可复现构建说明/脚本，记录 IDE 和编译器版本，补 CI 或本机脚本做编译、map 中 FLASH/RAM/栈余量检查，并对自有代码开启更严格的告警。`.gitignore` 已排除常见构建产物，但 `fixed_vqf_disasm.txt`、`wch_link_manual.txt` 等跟踪文件的来源/更新规则宜文档化。
- **配置文档一致性**：`User/fixed_vqf.h:12-15` 注释称 motion bias disabled，而其宏实际为 `1U`；`User/main.c:3` 文件描述仍称 SPI diagnostic + LED，均应更正。把采样率、量程和输出格式的组合配置集中管理，并用编译期检查拒绝不支持的组合。

## 推荐实施顺序

1. **先建基线**：在当前硬件上记录 `vqf_timer_ticks`、`vqf_update_count`、`vqf_missed_count`、CAN 各计数及执行时间峰值；保存一次 map 和一段 CAN/UART 实测结果。
2. **修复 P0**：先为转换/编码补边界单测，再修改溢出处理、SPI 错误传播、定时事件消费；每项独立提交与回归。
3. **校准时间基准**：测量 2 kHz/备用模式的 DRDY 与实际输出更新率，依据实测重新配置定时与滤波，进行静止漂移和已知旋转测试。
4. **改造诊断和恢复**：区分“已提交/已发送”、跳过样本/总线故障，补故障恢复用例；最后再拆模块、统一文档与自动化构建。

**未完成的验证**：本次未运行新编译、单元测试或真机实验；没有声称现在的固件必然超时、丢帧或实际姿态精度不达标。

## 修复进展（2026-09-23）

已修改：数值转换饱和与计数；跳过发生饱和的加速度校正；SPI 错误传播和恢复；原子地取走定时标志；1 kHz 模式从 2 kHz 传感器档位取新数据；按 CAN 邮箱完成状态统计发送成功；传感器故障时停止发送旧数据并定期重试；修正不准确的注释。详见 README 与 tests。

验证结果：主机 GCC 数值边界测试通过；WCH RISC-V GCC 对默认配置、1 kHz 配置和可选大量程配置均完成编译与链接；`git diff --check` 通过。没有进行下载、烧录或真机运行。

仍待完成：模块拆分、独立高精度耗时统计、故障上下文持久化、真机 SPI/CAN/DRDY 故障注入和精度回归。交叉编译通过不能代替真机测量。
